<?xml version="1.0" encoding="UTF-8" ?>
<rss version="2.0">
<channel>
<title>Help Wanted</title>
<description>Help Wanted Issues</description>
<link>https://lbonanomi.github.io/workspace/feed.xml</link>
<item>	<title>Help wanted to add more unit tests</title>
	<link>https://github.com/Autodesk/go-awsecs/issues/22</link>
	<description><![CDATA[ <pre></pre> ]]></description>
</item>
<item>	<title>Help wanted to add more unit tests</title>
	<link>https://github.com/Autodesk/go-awsecs/issues/22</link>
	<description><![CDATA[ <pre></pre> ]]></description>
</item>
<item>	<title>WANT_UFE_BUILD scattered throughout build process</title>
	<link>https://github.com/Autodesk/maya-usd/issues/589</link>
	<description><![CDATA[ <pre><gt;lt;p><gt;lt;strong>Describe the issue<gt;lt;/strong>
<gt;lt;strong>WANT_UFE_BUILD<gt;lt;/strong> can wind up controlling vtable layout and availability of functions.
Currently it’s usage is scattered throughout CMake and headers in mayaUsd, hdMaya, mayaToHydra, AL-usdmaya. Usually as <gt;lt;strong>PRIVATE<gt;lt;/strong> sometimes as <gt;lt;strong>PUBLIC<gt;lt;/strong> (#583).<gt;lt;/p>
<gt;lt;p>Seems better to be held in a spot where all sub-projects consume the value, and anyone building against a distribution can ingest the MACRO properly (i.e. in an installed CMake target).<gt;lt;/p></pre> ]]></description>
</item>
<item>	<title>:gh release create: Make the error message more descriptive, if <tag> isn't passed.</title>
	<link>https://github.com/cli/cli/issues/2327</link>
	<description><![CDATA[ <pre><gt;lt;h3 id="describe-the-feature-or-problem-youd-like-to-solve">Describe the feature or problem you’d like to solve<gt;lt;/h3>
<gt;lt;p>When <gt;lt;code>gh release create<gt;lt;/code> is run, without any arguments, the output received is: <gt;lt;code>requires at least 1 arg(s), only received 0<gt;lt;/code>. This is not helpful to a user who is not aware of the command (until <gt;lt;code>gh release create --help<gt;lt;/code>).<gt;lt;/p>
<gt;lt;h3 id="proposed-solution">Proposed solution<gt;lt;/h3>
<gt;lt;p>Instead of the not-so-useful message, a user can receive a message like: <gt;lt;code>could not create: no tag name provided<gt;lt;/code>. This will help to describe the user about the command at command execution time, rather than using <gt;lt;code>gh release create --help<gt;lt;/code>.<gt;lt;/p></pre> ]]></description>
</item>
<item>	<title>when GITHUB_TOKEN is set, print better messaging during gh auth login</title>
	<link>https://github.com/cli/cli/issues/2304</link>
	<description><![CDATA[ <pre><gt;lt;p>Currently we let a user get through two prompts of <gt;lt;code>gh auth login<gt;lt;/code> when GITHUB_TOKEN is set, then unceremoniously tell them that the token is read-only.<gt;lt;/p>
<gt;lt;p>We should proactively notice that situation when <gt;lt;code>gh auth login<gt;lt;/code> is run and give clear messaging about unsetting the token in order to use the command.<gt;lt;/p>
<gt;lt;p>Initially we thought only power users would be using GITHUB_TOKEN as an opt-in choice, making this fine; but in Codespaces GITHUB_TOKEN is being set without users ever knowing about it.﻿<gt;lt;/p></pre> ]]></description>
</item>

</channel>
</rss>
